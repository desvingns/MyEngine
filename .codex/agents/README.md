# Codex Agents

Repo-local agent shims may live here. Canonical contracts are in `docs/agentic/AGENT_CONTRACTS.md`.

The MyEngine Codex adapters use the canonical contract and do not maintain a second agent roster:
`codex-plugins/me-dev` and `codex-plugins/me-spec` are the registered skill entry points.
