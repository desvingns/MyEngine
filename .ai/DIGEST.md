# MyEngine Intake Digest

Regenerated by me-docs at every close-out. Compact intake source - read this first;
open the full docs (STATE.md, .ai/handoff.md, Plane/) only when this is stale or
insufficient. Last updated: 2026-07-28 (ENG-009 accepted after all runner gates and final verifier; next backlog sequence is not yet selected).

## Where we are

- Phases 00-14 complete. Signal Garden SG-001/002/003(+follow-up)/004(+follow-up)/005 done.
- Second game spec: MyTD (bundle at D:/Pet/MyTD/spec). MTD-001 is closed as a duplicate of SG-002;
  MTD-002 is closed by generic content-driven tower cost gating plus `SandboxTowerCostGatingTest`;
  MTD-003 is closed by content-defined upgrade tiers, `UpgradeTowerCommand`, and sandbox save v3
  branch/tier persistence; MTD-004 is closed by data-defined difficulty content and deterministic
  pre-tick scaling; ENG-024 moved command DTOs to `engine-core` and made InputAdapter UI state
  explicit; ENG-005 now supplies validated JSON map content and v4 map/content save identity;
  ENG-014 adds data-defined terminal rules, immutable snapshot summaries, and save v5 terminal
  persistence; MTD-005 adds an Android Canvas consumer of immutable `RenderFrame` plus
  `MotionEvent` -> `InputAdapter` touch handling. ENG-026 adds an Android-local 20 Hz
  Choreographer `TickScheduler`, a SurfaceView that renders the latest immutable `RenderFrame`,
  command-queue-only input, and pause/save/Bundle restoration preserving pending commands and the
  next command ID. ENG-027 adds a localized content-derived HUD snapshot, Android build/select/
  upgrade panels through `InputAdapter`, deterministic per-tower damage/kills, and save v6 metrics
  persistence. ENG-002 replaces precomputed enemy paths with deterministic `GoalField` routing;
  all-spawn prospective placement validation, `occupied_by_enemy`, and same-tick reroutes are
  covered. Its v6-derived field canonicalizes legacy paths after restore. Full tests, replay,
  save-compat, and benchmark pass; device/layout/performance checks from ENG-027 remain
  manual-pending. ENG-013 adds deterministic `SellTowerCommand`: required content-defined
  `sellRefundRatio` (`0..1` inclusive), per-resource floor refund of base plus applied tier spend,
  atomic capacity rejection, occupancy/metrics removal, and a same-tick goal-field rebuild. Pending
  sells persist through the existing queue encoding; `SAVE_VERSION` remains `6`. ENG-008 adds pure
  deterministic `first`/`last`/`nearest`/`strongest`/`weakest` targeting with entity-id tiebreak,
  content defaults (v1 missing `targetingMode` -> `NEAREST`), a queued per-tower mode command, and
  immutable HUD mode projection. Save v7 persists tower modes and pending mode-switch commands;
  v1-v6 saves migrate through the content default.
- ENG-015 adds Android-local `0x`/`1x`/`2x`/`4x` presentation pacing and callback-only HUD controls;
  fixed-tick simulation, per-tick trajectory, replay hashes, and save boundaries remain unchanged.
- ENG-030 adds deterministic next-wave composition/countdown HUD projection, typed
  `CallWaveEarlyCommand`, content-defined `resourceId + amount` bonus validation, strict
  pre-schedule execution, active-wave/boundary rejection, and `SandboxSaveCodec` v8 with v1-v7
  migration. Full tests and all requested gates pass; replay hashes remain canonical
  `12a65fd2b87593cf` and kill `bb37eefc1903cc77`. Balance review returned partial: current packs
  are valid and contain no hardcoded bonus; its schema-documentation gap was closed in this
  close-out, and the optional bonus remains unconfigured pending an approved balance value.
- ENG-028 adds optional pack-relative sprite/atlas refs for tiles, towers, tower tiers, enemies,
  and minimal buildings; loader diagnostics validate files/path containment/atlas keys, while
  opaque refs cross immutable snapshots to desktop/Android consumers and omitted/missing refs use
  deterministic palette fallback. Full tests, content validation, replay, save-compat, benchmark,
  Android assemble, focused desktop/render tests, and desktop render smoke pass; replay hashes remain
  `12a65fd2b87593cf` and `bb37eefc1903cc77`. No save schema or dependency edge changed.
- ENG-009 adds optional tower splash content with stable entity-id-ordered Manhattan AoE and one
  integer-only per-ring falloff rule. Immutable source/target/tick `ShotEvent`/`HitEvent` lists are
  transient snapshot presentation data from the latest completed tick only, excluded from save and
  stable-hash state. `SAVE_VERSION` remains `8`; no shipped pack gained balance values. Full tests,
  projects, content validation (2 packs), replay, save-compat, benchmark, Android assemble, balance
  review, and final verifier pass; replay hashes remain `12a65fd2b87593cf`/`bb37eefc1903cc77`.
- DX-008 is accepted through ADR-0003: flat definitions remain `.properties`; nested maps use
  additive `maps.json`. The equivalent sandbox fixture preserves both canonical replay hashes.
- Pipeline v0.2.0: reviewer matrix, me-scout, mandatory telemetry (incl. failures),
  reflect cadence (failed run or every 5th event), proposal queue .ai/proposals/,
  gap dedup + .claude/specs/ENGINE_ROADMAP.md.

## Next exact action

Select the next backlog item after completed ENG-009. The current roadmap ends its explicit P1
sequence at ENG-009 and does not define a unique successor; perform backlog sequencing first.

## Active specs / roadmap

- ENG-014 win/lose conditions and run summary are done (2026-07-16).
- MTD-005 is done/accepted (2026-07-16): Android Canvas projects and draws `RenderFrame`; tap/pan/
  pinch flow through `InputAdapter`; caller-owned command IDs and submit/step stay outside render.
- ENG-026 is done/accepted (2026-07-16): SurfaceView consumes the latest immutable `RenderFrame`;
  Choreographer drives an Android-local 20 Hz fixed-tick policy; input queues commands only; pause
  save/Bundle restore preserves pending commands, next command ID, and replay continuity.
- ENG-027 is done/accepted (2026-07-18): immutable HUD data is content-derived and localized;
  Android build/select/upgrade UI emits queued commands through `InputAdapter`; defense metrics track
  deterministic per-tower actual damage/kills; save v6 persists them. Replay hashes remain unchanged.
- ENG-002 is done/accepted (2026-07-18): deterministic core-outward goal field, all-spawn
  prospective path rejection, occupied-tile rejection, and same-tick reroutes; save field is
  derived/canonicalized. Replay hashes: canonical `463d87684ca6cbee`, kill `40c7bda7e3bc1316`, maze
  `ed0354584405ec49`; final 64x64 rebuild metric `4.1904 ms`.
- ENG-013 is done/accepted (2026-07-18): sale refunds the validated content-defined ratio of
  cumulative base/applied-tier spend per resource with floor rounding; accepted sales free the tile,
  remove metrics, and rebuild the goal field before same-tick movement. Queue save coverage includes
  pending sells; `SAVE_VERSION` remains `6`. Final gates/review/verifier pass; canonical/kill replay
  hashes are `463d87684ca6cbee` / `40c7bda7e3bc1316`.
- ENG-008 is done/accepted (2026-07-18): pure selector modes use entity-id tiebreak; content
  defaults missing v1 values to `NEAREST`; `SetTowerTargetingModeCommand` is queued; HUD exposes
  the active mode; save v7 migrates v1-v6 and persists pending mode switches. Full suite and all
  gates pass; replay hashes are canonical `12a65fd2b87593cf` and kill `bb37eefc1903cc77`; benchmark
  is canonical `432 ms`, kill `79 ms`, goal-field rebuild `5.3678 ms`; final verifier passes.
- ENG-015 is done/accepted (2026-07-21): presentation-only Android speed modes, per-tick trajectory
  parity, and HUD layout bounds pass. Full tests, Android unit/assemble, content validation, replay,
  save-compat, and benchmark pass; replay hashes are canonical `12a65fd2b87593cf` and kill
  `bb37eefc1903cc77`; benchmark is canonical `328 ms`, kill `66 ms`, rebuild `4.1305 ms`.
- ENG-028 is done/accepted (2026-07-28): sprite/atlas refs and minimal building schema are
  content-validated and render-consumer tested; Android AssetManager has compile/assemble coverage
  but no device/instrumented fixture, and the full PROC-009 screenshot/golden lane remains manual.
- ENG-009 is done/accepted (2026-07-28): optional splash content has stable integer Manhattan AoE;
  transient immutable shot/hit events cross the snapshot without entering saves or hashes. All
  runner gates, balance review, and final boundary checks pass; save version remains `8`.
- EG-006 MyTD content pack remains in the MyTD bundle, not a MyEngine backlog item yet.
- PROC-001..010 process cards filed 2026-07-04.
- PROC-002 is done through ADR-0004 (composite build + pinned engine SHA). MySD adds ENG-036
  reusable runtime/session and PROC-015 reference evidence bridge as backlog cards; no gameplay
  demand is counted before its Gate 1.

## Known risks / blockers

- ENG-027 manual limitations: device/emulator build/select/upgrade/lifecycle smoke; non-default
  fontScale and long-label visual coverage; FrameMetrics/JankStats/allocation profiling. Save v6
  malformed-input hardening remains for missing `towerMetrics` and duplicate tower ids.
- ENG-026 device/performance proof is pending: validate tap, pan, pinch, and pause/recreate with a
  pending command plus command-ID/hash continuity on a device/emulator. Capture FrameMetrics/
  JankStats and Allocation Tracker evidence before any smoothness or frame-budget claim.
- MTD-005 is accepted, not device-validated: no emulator/device smoke verified tap-build, drag,
  pinch, draw order, or debug recreation/pending-command continuity.
- MTD-005 known performance risk: every redraw creates a snapshot/frame and intermediate primitive
  lists; FrameMetrics/JankStats and Allocation Tracker profiling are still required before a frame-
  budget claim.
- ENG-005 low, non-blocking: the Android module packages `maps.json`, but `SandboxGame.loadRegistry()`
  still seeks a filesystem path rather than `AssetManager`; this remains unverified on device.
- ENG-005 low, non-blocking: `BalanceDeltaReport` has no map-local resource-node or geometry metrics.
- MTD-004 low, non-blocking follow-up: `difficultyId` is not serialized; restore requires the same
  effective difficulty-resolved registry.
- Camera.clamped() vs screenToTile edge mismatch (low, deferred).
- Gradle 10 deprecation warning from AGP internals (builds pass).
- SG-004 on-device Bundle round-trip still device-pending (no device/emulator available);
  JVM-covered proof is sufficient for now.
- SG-004 follow-up low-priority items (2026-07-05, non-blocking): no persisted CommandId-issuing
  counter (forward-looking, no current caller); no engine-core-unit determinism test for
  pending()/submitAll() (covered end-to-end instead); pendingCommands encoding shares the
  pre-existing delimiter-collision assumption with other properties-line fields in the same codec.
- SG-005 report thresholds are suspicious-value metadata only, not final balance policy.
- ENG-015 follow-ups: device/instrumentation lifecycle/tap smoke and FrameMetrics/JankStats remain
  pending; `200x600` selected-panel overflow is a manual risk; pre-existing `pausedSave` rollback
  risk is outside scope; `0x` keeps an idle HUD redraw active as an accepted CPU/battery trade-off.
- ENG-030 follow-ups: existing packs intentionally omit optional early-call bonus values because no
  balance value was approved; the data-driven path is synthetic-test proven. Low per-snapshot HUD
  allocation/device profiling remains pending, and the existing save delimiter assumption remains
  documented without a code fix.
- ENG-028 follow-ups: Android AssetManager runtime/device and PROC-009 golden screenshot checks are
  pending. Conditional reviewer agents could not be spawned because the agent-thread limit was
  reached; local read-only simulation/render/save/Android/content boundary review passed.

## Invariants (non-negotiable)

- Simulation stays Android/render-free and deterministic (fixed tick, seeded RNG,
  stable ordering); render consumes immutable snapshots only.
- Content is external/data-driven/versioned; saves versioned from v1.
- Current replay hashes: canonical 12a65fd2b87593cf, kill bb37eefc1903cc77; maze golden
  ed0354584405ec49.
- Canonical process: docs/agentic/*; adapters are thin; writer != reviewer.

## Environment

- JAVA_HOME=C:\Program Files\Android\Android Studio\jbr; ANDROID_HOME=%LOCALAPPDATA%\Android\Sdk
